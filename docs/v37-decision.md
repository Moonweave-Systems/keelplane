# V37 README Public Page Decision

Decision: keep

V37 promotes the V36 README benchmark graph into tracked assets:

- `assets/dwm-live-benchmark.svg`
- `assets/dwm-live-benchmark.json`

The README now renders the graph directly from `assets/` and keeps the benchmark
claim tied to `report.json.graph_metrics` through the published asset record.

This decision covers README page polish and graph publishing only. It does not
claim external benchmark authority, model superiority, hosted evaluation, or
autonomous completion.
