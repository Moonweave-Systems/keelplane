# V86 Decision

Decision: keep.

V86 keeps the Depone brand decision because it gives the project a stronger
public name without breaking the existing DWM Core, CLI, fixture, skill, and
artifact contracts.

## Evidence

- `README.md` now leads with `Depone`.
- `docs/dwm-branding.md` defines `Depone` as the product brand and `DWM Core`
  as the internal deterministic engine.
- `assets/dwm-hero.svg` names `Depone` and `Powered by DWM Core`.
- The installed Codex skill name is `depone`.

This does not claim autonomous execution, benchmark improvement, or agent
superiority. It is a branding alignment slice only.
